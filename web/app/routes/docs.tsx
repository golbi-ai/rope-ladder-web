import styles from "./site.module.css";
export function meta(){return [{title:"rope-ladder documentation"}]}
export default function Docs(){return <main className={`wrap ${styles.page}`}><p className={styles.eyebrow}>Documentation</p><h1>A portable curriculum for a living codebase.</h1><p>rope-ladder makes an unfamiliar repository teachable without modifying that repository. It uses an AgentRouter-supported coding agent at the lowest available sampling setting, researches factual claims, and keeps the result in an independent curriculum workspace.</p><h2>Core commands</h2><ul><li><code>init</code> creates the independent local-Git curriculum workspace.</li><li><code>lesson-plan</code> indexes source, generates references and standalone diagrams, and builds the prerequisite DAG and focused units.</li><li><code>ta</code> asks a reflection question for the next lesson and writes a private journal outside the workspace.</li><li><code>publish</code> explicitly opens a fork-based pull request to the public curriculum catalog.</li><li><code>mcp install</code> registers the bundled local MCP server with supported agent clients.</li><li><code>upgrade</code> verifies and installs the newest direct release; package-managed installs continue through their package manager.</li></ul><h2>Privacy boundaries</h2><p>Source transfer to a provider is an explicit per-run action. Public publication is a different explicit action. The catalog payload is allowlisted: it includes rendered learning artifacts, but excludes journals, caches, configuration, workspace state, conflict proposals, and credentials.</p><h2>Curriculum artifacts</h2><p>Every completed lesson plan contains concise domain and implementation references, a meaningful-entity reference, offline architecture and entity-relationship HTML diagrams, a human-readable lesson plan, and a machine-readable manifest for the teaching assistant.</p></main>}
