import styles from "./site.module.css";
export function meta(){return [{title:"Install rope-ladder"}]}
export default function Install(){return <main className={`wrap ${styles.page}`}><p className={styles.eyebrow}>Install</p><h1>Bring a guide into the next unfamiliar repository.</h1><p>rope-ladder ships as a desktop Go CLI for macOS, Linux, and Windows. Use a release binary when available, or install from Go.</p><h2>Go install</h2><pre className={styles.code}>go install github.com/golbi-ai/rope-ladder/cmd/rope-ladder@latest</pre><h2>Create a separate curriculum</h2><pre className={styles.code}>rope-ladder init --source /path/to/codebase /path/to/curriculum{`\n`}rope-ladder lesson-plan --allow-source-transfer /path/to/curriculum</pre><p>The curriculum directory is a separate local Git repository. Generated files are shareable; teaching-assistant reflections remain in your private local state directory.</p><h2>Share a reviewed candidate</h2><pre className={styles.code}>rope-ladder publish --metadata ./metadata.yaml --slug your-codebase --confirm-public /path/to/curriculum</pre><p>Publishing uses your existing GitHub CLI login to open a pull request from your fork. It requires a public-source attestation and never uploads private progress or local workspace state.</p></main>}
